schema: jii.ambyte-schedule/v1-draft
name: neg
jobs:
  j:
    on: { every: 5m }
    when: { window: { from: sunrise, to: sunset, unresolved: maybe } }
    steps: [ { uses: device/log, with: { message: hi } } ]

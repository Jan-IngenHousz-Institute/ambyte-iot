.*/
!.github/
.env
__pycache__/
build/
/dist/
/node_modules/
/release-assets/
/release-preview.log
/release-preview.md
/releases/
temp_inipreview.txt
dom_ludo_prototype_ambyte_thing_certs/
docs/ble-provisioning-cmd.txt
flashed_macs.csv
open-jii/
ambit-1/
device_certs/
pip/
/flash/
planning-internalDocs/

# Claude Code personal/local settings (machine-specific permissions)
.claude/settings.local.json
databricks/
log_debug/
dataset.csv

# PlatformIO writes one full sdkconfig per additive bench environment. The
# maintained inputs are sdkconfig.bench*.defaults; these generated files are not.
sdkconfig.bench
sdkconfig.bench-*
graphify-out/

# PyInstaller writes its spec next to the invocation (flash-gui-build.yml);
# build/ and /dist/ are already ignored above.
ambyte-flash-gui.spec

# Configure-time generated headers belong in the build dir; the ESP-IDF
# requirements scan used to leak them into the repo root (cmake fixed, this
# is belt-and-braces).
default_yaml_embed.h
